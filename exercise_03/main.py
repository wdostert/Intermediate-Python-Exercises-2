import numpy as np

random_nums = np.random.rand(10)

mean = np.mean(random_nums)
median = np.median(random_nums)
standard_deviation = np.std(random_nums)

print("Random Numbers:", random_nums, " Mean:", mean, " Median:", median, " Standard Deviation:", standard_deviation)

